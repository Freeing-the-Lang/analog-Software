name: "🧠 Analog-AI — Meaning Curve Engine (v4.4)"

on:
  push:
    branches: [ main ]
  pull_request:
    types: [ closed ]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: analog-ai-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # ============================================================
  # BUILD JOB — 3OS Meaning Engine
  # ============================================================
  analog-meaning-build:
    name: "🧠 Build & Run Meaning Curve Engine"
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      # ---------------------------------------
      # Checkout + Rust
      # ---------------------------------------
      - name: "📦 Checkout"
        uses: actions/checkout@v4

      - name: "🦀 Setup Rust"
        uses: dtolnay/rust-toolchain@stable

      # ---------------------------------------
      # Install NASM
      # ---------------------------------------
      - name: "📚 Install NASM (Linux)"
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y nasm

      - name: "📚 Install NASM (macOS)"
        if: runner.os == 'macOS'
        run: |
          brew install nasm

      - name: "📚 Install NASM (Windows)"
        if: runner.os == 'Windows'
        run: choco install nasm -y

      # ---------------------------------------
      # Build Engine
      # ---------------------------------------
      - name: "⚙️ Build Meaning Engine"
        run: cargo build --release

      - name: "🧪 SpongeLang Semantic Tests"
        run: cargo test --all --verbose

      # ---------------------------------------
      # Meaning Curve Engine (OS-specific execution)
      # ---------------------------------------
      - name: "🧠 Run Meaning Engine (Linux/macOS)"
        if: runner.os != 'Windows'
        shell: bash
        run: |
          mkdir -p analog-output
          ./target/release/analog-ai-meaning-engine "samples/sample.sp" > analog-output/curve.txt

      - name: "🧠 Run Meaning Engine (Windows)"
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          mkdir analog-output
          .\target\release\analog-ai-meaning-engine.exe "samples/sample.sp" | Out-File -Encoding utf8 analog-output\curve.txt

      # ---------------------------------------
      # SHA256 + ProofLedger
      # ---------------------------------------
      - name: "🔐 SHA256 Proof"
        shell: bash
        run: |
          sha256sum analog-output/curve.txt > analog-output/sha256.txt 2>/dev/null || \
          shasum -a 256 analog-output/curve.txt > analog-output/sha256.txt

      - name: "📜 Generate ProofLedger"
        shell: bash
        run: |
          mkdir -p proof
          {
            echo "Analog-AI ProofLedger"
            echo "timestamp=$(date -u)"
            echo "commit=${GITHUB_SHA}"
            echo "os=${RUNNER_OS}"
            echo "source=samples/sample.sp"
            echo "curve=analog-output/curve.txt"
            echo "sha256=$(cat analog-output/sha256.txt)"
          } > proof/ledger.txt

      # ---------------------------------------
      # ZIP PACKAGING (NEW)
      # ---------------------------------------
      - name: "📦 Create ZIP Package"
        shell: bash
        run: |
          mkdir -p pkg
          cp -r analog-output pkg/
          cp -r proof pkg/
          cp samples/sample.sp pkg/
          cd pkg
          zip -r analog-ai-${{ matrix.os }}.zip .

      # ---------------------------------------
      # Upload Artifacts (ZIP)
      # ---------------------------------------
      - name: "📤 Upload Artifacts"
        uses: actions/upload-artifact@v4
        with:
          name: analog-ai-${{ matrix.os }}
          path: |
            pkg/
            analog-output/
            proof/
            samples/sample.sp

  # ============================================================
  # RELEASE JOB
  # ============================================================
  create-release:
    name: "🚀 Auto Tag & Release"
    runs-on: ubuntu-latest
    needs: analog-meaning-build

    permissions:
      contents: write

    steps:
      - name: "📦 Checkout"
        uses: actions/checkout@v4

      # -----------------------------
      # Version
      # -----------------------------
      - name: "🔢 Generate Version"
        id: version
        run: |
          VERSION="v$(date +'%Y%m%d-%H%M%S')"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      # -----------------------------
      # Tag Creation
      # -----------------------------
      - name: "🏷️ Create Tag"
        run: |
          git tag ${{ steps.version.outputs.version }}
          git push origin ${{ steps.version.outputs.version }}

      # -----------------------------
      # Create Release
      # -----------------------------
      - name: "🚀 Create GitHub Release"
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: "Analog-AI — ${{ steps.version.outputs.version }}"
          body: |
            🧠 **Analog-AI Meaning Curve Engine Auto Release**
            - Multi-OS build (Linux, macOS, Windows)
            - ZIP packages included
            - ProofLedger + SHA256 included

      # -----------------------------
      # Download All Artifacts
      # -----------------------------
      - name: "📥 Download All Artifacts"
        uses: actions/download-artifact@v4

      # -----------------------------
      # Upload to Release (404 FIXED)
      # -----------------------------
      - name: "📤 Upload All Files to Release"
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.version.outputs.version }}
          files: |
            analog-ai-ubuntu-latest/**
            analog-ai-macos-latest/**
            analog-ai-windows-latest/**
