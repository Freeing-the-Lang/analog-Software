# Digital → Analog Thinking Demo
# SpongeLang Sample File (sample.sp)

# 1) 디지털식 분기 → 아날로그 해석
if score > 80 -> pass
else -> retry

# 2) 아날로그 매핑
emotion_level = map(feel, 0..1 -> calm..focus)

# 3) 의미 기반 함수
fn blend(a, b) = (a * 0.6) + (b * 0.4)

# 4) 연속적 의미 흐름
temperature = smooth(prev -> next)

# 5) 디지털 → 아날로그 사고 실험
concept = "digital-to-analog"
